<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/widget_container"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:background="@drawable/widget_background"
    android:padding="12dp"
    android:minWidth="215dp">

    <!-- Ligne d'info : poignée de déplacement, chrono, revenu, action secondaire.
         minHeight fixe pour que le widget ne change pas de taille quand le
         bouton secondaire apparaît / disparaît. -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:minHeight="36dp"
        android:orientation="horizontal"
        android:gravity="center_vertical">

        <View
            android:id="@+id/poignee"
            android:layout_width="18dp"
            android:layout_height="4dp"
            android:layout_marginEnd="10dp"
            android:background="@drawable/badge_background" />

        <TextView
            android:id="@+id/tv_temps"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:text="00:00"
            android:textColor="#FFFFFF"
            android:textSize="20sp"
            android:textStyle="bold" />

        <TextView
            android:id="@+id/tv_revenu"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginStart="8dp"
            android:text="0,00 €"
            android:textColor="#FFD700"
            android:textSize="15sp"
            android:textStyle="bold"
            android:visibility="gone" />

        <!-- Action secondaire : rare et parfois destructive, donc petite cible
             distincte, à l'écart du bouton principal. -->
        <TextView
            android:id="@+id/btn_secondaire"
            android:layout_width="36dp"
            android:layout_height="36dp"
            android:layout_marginStart="10dp"
            android:background="@drawable/badge_background"
            android:gravity="center"
            android:text="⏸"
            android:textColor="#FFFFFF"
            android:textSize="15sp"
            android:visibility="gone" />
    </LinearLayout>

    <!-- Action principale : l'essentiel du widget, pressée à chaque course. -->
    <Button
        android:id="@+id/btn_action"
        android:layout_width="match_parent"
        android:layout_height="68dp"
        android:layout_marginTop="10dp"
        android:backgroundTint="#FFFFFF"
        android:text="COMMENCER"
        android:textColor="#10131F"
        android:textSize="16sp"
        android:textStyle="bold"
        android:elevation="4dp" />

</LinearLayout>
